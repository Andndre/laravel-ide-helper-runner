# Laravel IDE Helper Runner

This extension listens to `.php` file saves and automatically runs Laravel IDE Helper commands to keep your auto-completion signatures up-to-date.

## Configuration

You can change the command that runs in the extension settings.
